# Database Connection
# For Filess.io (free 10MB MySQL):
# 1. Go to https://filess.io/
# 2. Sign up for free
# 3. Create a MySQL database
# 4. Copy the connection string
DATABASE_URL=mysql://username:password@host:port/database?ssl={"rejectUnauthorized":true}

# Example for Filess.io:
# DATABASE_URL=mysql://user_abc123:pass_xyz789@mysql-abc.filess.io:3306/db_jurista?ssl={"rejectUnauthorized":true}

# Alternative: Aiven (requires credit card but free tier available)
# DATABASE_URL=mysql://username:password@host:port/database?ssl-mode=REQUIRED
